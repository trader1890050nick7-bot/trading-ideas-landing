# Trading Ideas Landing Page

Лендинг для подписки на еженедельные торговые идеи и сигналы.

## Возможности

- 📈 Адаптивный дизайн
- ✅ Валидация email на клиенте и сервере
- 💾 Сохранение подписок в JSON файл
- 🔒 CORS защита
- ⚡ Express.js backend

## Установка

1. Клонируйте репозиторий
2. Установите зависимости:
```bash
npm install
```

## Запуск

### Запуск сервера

```bash
npm start
```

Сервер будет доступен по адресу: `http://localhost:3000`

### Открытие в браузере

Откройте `http://localhost:3000` в браузере

## API Endpoints

### POST /subscribe
Подписка на рассылку

**Request:**
```json
{
  "email": "user@example.com"
}
```

**Response (успех):**
```json
{
  "success": true,
  "message": "Подписка успешно оформлена! Проверьте ваш email"
}
```

**Response (ошибка):**
```json
{
  "success": false,
  "message": "Некорректный формат email"
}
```

### GET /subscribers
Получить список всех подписчиков (для тестирования)

**Response:**
```json
{
  "success": true,
  "count": 2,
  "subscribers": [
    {
      "email": "user@example.com",
      "subscribedAt": "2026-01-06T12:00:00.000Z"
    }
  ]
}
```

## Структура проекта

```
trading-ideas-landing/
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js             # Клиентский JavaScript
├── server.js          # Express сервер
├── package.json       # Зависимости
├── emails.json        # Хранилище подписок (генерируется автоматически)
└── README.md          # Документация
```

## Технологии

- HTML5
- CSS3 (Grid, Flexbox, Animations)
- Vanilla JavaScript
- Node.js
- Express.js
- CORS

## Лицензия

ISC
